@model SearchApp.Models.Types

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Types</h4>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.FreebaseName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FreebaseName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Domains)
        </dt>
        <dd>
            <ul>
                @foreach (var item in Model.Domains)
                {
                    <li>@item.Name</li>
                }
            </ul>
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Attributes)
        </dt>
        <dd>
            <ul>
                @foreach (var attribute in Model.Attributes)
                {
                    <li>@attribute.Name</li>
                }
            </ul>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
